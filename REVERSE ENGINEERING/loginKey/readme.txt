the directory named contestant code is what is uploaded to CTFD for contestants to down load to solve the challenge.

The Directory name code is what is to be uploaded to the ncat or docker, this directory contains code that has the Challenge flag stored in plain text


i have attached a docker file in the code directory, please test it worked fine on my end 

