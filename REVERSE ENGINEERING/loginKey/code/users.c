//users.c: michael Aaron N 
#include "users.h"

User users[] = {
    {"0x426162792058", "EspionageCTF{CrAcK3d!}"} // login key is the hex value this is to diguse it also it my dc username :) 
};

int num_users = sizeof(users) / sizeof(users[0]);
